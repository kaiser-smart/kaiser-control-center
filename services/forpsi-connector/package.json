{
  "name": "company-forpsi-connector",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev --local"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "fast-xml-parser": "^5.11.1",
    "ical.js": "^2.2.1",
    "imapflow": "^2.0.6",
    "jose": "^6.2.12",
    "mailparser": "^3.9.28",
    "nodemailer": "^10.0.10",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "wrangler": "^4.138.0"
  }
}
